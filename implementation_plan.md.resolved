# VentureAgent MVP 实施计划

## 产品目标
开发 VentureAgent 的最小可行性产品 (MVP)，主要目标是搭建前后端的基础链路，不引入过重的数据库（如 Neo4j 和 PostgreSQL），仅保留核心的 LangGraph 对话控制流和前端的简易 UI 交互，确保项目代码能够跑起来，供初步演示和体验。

## Proposed Changes (拟议变更)

### 后端 (Python/FastAPI + LangGraph)
考虑到初稿需要轻量化且快速运行，后端的架构如下：
- **框架搭建**: `backend/app/main.py` 作为入口，引入 FastAPI 框架并配置跨域 (CORS)。
- **智能体结构**: 在 `backend/app/agent/graph.py` 中编写简单的 LangGraph 逻辑。包含一个简化的 Router 和一个模拟的“学习辅导 Agent (Student Learning Tutor)”，在 MVP 中仅作基于规则或是简单提示词的回应。
- **依赖管理**: 提供 `requirements.txt` 以供一键安装依赖（如 `fastapi`, `uvicorn`, `langgraph`, `langchain` 等）。

### 前端 (React + Ant Design)
基于技术架构的要求，前端使用 React 18 搭配 Ant Design，为了快速启动使用 Vite 构建：
- **工程初始化**: 创建 `frontend` 目录，生成基于 Vite 的 React 模板。
- **页面构建**: 实现一个基础的聊天页面组件，包含历史消息列表和底部的气泡输入框。运用 Ant Design 的组件群（如 `List`, `Input`, `Button`）。
- **接口联调**: 实现与后端 `/api/chat` 接口的通信，完成一问一答的数据闭环。

## Verification Plan (验证计划)

### 自动化/命令行验证
1. **后端验证**:
   命令行执行：`cd backend && pip install -r requirements.txt && uvicorn app.main:app --reload`
   访问 `http://localhost:8000/docs` 验证 FastAPI 的 Swagger API 文档是否正常加载。
2. **前端验证**:
   命令行执行：`cd frontend && npm install && npm run dev`
   终端输出正确的 Localhost 地址，表示 Vite 开发服务器运行成功。

### 手动验证
- 启动前后端服务后，打开前端页面在浏览器中的对应地址（如 `http://localhost:5173`）。
- 在聊天框中输入“你好，请问你是谁？”，点击发送。
- 等待后端 LangGraph 处理完毕并返回消息“我是你的辅导学习 Agent...”，并且 UI 显示正常，即表明前后端链路全面打通。
